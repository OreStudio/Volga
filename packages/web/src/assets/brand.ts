import splash from './ore-studio-splash.png';

/**
 * The landing page's hero image.
 *
 * The same artwork the main site uses, so a link from there arrives somewhere
 * that looks like the same project.
 */
export const heroSplash: string = splash;

/** The project site. Linked from the header and from the landing page. */
export const PROJECT_SITE = 'https://orestudio.github.io/OreStudio/';
